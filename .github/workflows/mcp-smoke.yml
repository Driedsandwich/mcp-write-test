name: MCP Smoke

on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm manual workflow dispatch
        run: echo "GitHub MCP workflow dispatch smoke test"
